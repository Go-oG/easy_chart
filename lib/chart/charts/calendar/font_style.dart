import 'package:flutter/material.dart';

class FontStyle {
  final double fontSize;
  final FontWeight fontWeight;
  final Color color;
  const FontStyle(this.fontSize, this.color, {this.fontWeight = FontWeight.normal});
}
